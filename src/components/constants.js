import {Vector3} from 'three';

export const shapes = [
  'Box', 'Cone', 'Cylinder', 'Dodecahedron', 'Icosahedron', 'Octahedron', 'Sphere', 'Tetrahedron', 'Torus', 'Torus Knot'
];

export const initialCameraPosition = new Vector3(0, 0, 300);

export const hoverElementOutlineColor = '#aaaaaa';
export const selectedElementOutlineColor = '#ff0000';

export const halfPi = Math.PI / 2;
